#!/usr/bin/env bash
source /dev/stdin < <(cli shim)

main() {
        
    local path='.'
    for i in "$@"; do
        path="${path}/${i}"
    done

    (
        cd "$path"
        for entry in *; do 
            [ -d "$entry" ] && [ -x "$entry" ] && echo $entry
        done
    )
}

shim "$@"
