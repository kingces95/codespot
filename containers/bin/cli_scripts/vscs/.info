#!/usr/bin/env bash
source /dev/stdin < <(cli shim)
. $(dirname ${BASH_SOURCE})/vscs.sh

help() {
util::unindent << EOF
    Command
        vscs

    Global Arguments
        --help -h               : Show this message and exit.
        --self-test             : Runs a self test.
EOF
}

main() {
util::unindent << EOF
    version 0.1.0
    licence MIT
    description Visual Studio Code Server CLI.
EOF
}

test() (
    return
)

shim "$@"